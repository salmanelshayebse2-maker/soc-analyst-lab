# Screenshots

Captured from Windows PowerShell on September 9, 2026, while running this lab with Python 3.12.14.

- `01-detection-results.png`: evidence verification, a full detection run, and the resulting alerts.
- `02-tests-passing.png`: all 28 tests completing successfully.
- `03-powershell-investigation.png`: Sysmon record 2001 and its decoded command.

The screenshots are direct console captures, without added annotations or altered output. The capture session uses a short prompt and a script that displays each command before running it with the locally installed Python interpreter. The event data shown is synthetic, as described in the case brief.

Run the commands in the main README to reproduce these results. Test duration will vary by machine.
